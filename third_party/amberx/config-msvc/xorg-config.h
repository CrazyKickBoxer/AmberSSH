/* xorg-config.h — AmberX stand-in. The Xorg DDX's own config; the
 * device-independent core includes it in one place, and needs nothing from
 * it that dix-config.h does not carry. */
#ifndef AMBERX_XORG_CONFIG_H
#define AMBERX_XORG_CONFIG_H
#include "dix-config.h"
#endif
