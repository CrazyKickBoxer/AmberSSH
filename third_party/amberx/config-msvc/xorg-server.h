/* xorg-server.h — AmberX stand-in. Upstream generates this as the public
 * configuration for out-of-tree modules; in-tree sources that include it
 * only need the same definitions dix-config.h already provides. */
#ifndef AMBERX_XORG_SERVER_H
#define AMBERX_XORG_SERVER_H
#include "dix-config.h"
#endif
