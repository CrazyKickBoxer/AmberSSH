// resource.h — ids for the Win32 resource script (assets/AmberSSH.rc).
#pragma once

// The application mark. A multi-size icon (16 … 256) with PNG-compressed
// frames, extracted from the brand sheet with the black keyed out to alpha, so
// it reads on the taskbar, in Explorer, in Alt-Tab and inside the About box.
#define IDI_APPICON 101
